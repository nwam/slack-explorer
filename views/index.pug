extends layout

append title
  input(id="search-input" type="text" placeholder="Enter your username")
  button(type="submit" style="margin: 15px; padding-left: 5px; padding-right: 5px" onclick="onSearch()") Search
  div(id="search-output" style="color: red")

block content
  div(id="viewport" style="width: 100%; height: 100%; background: #262626")

append scripts
  script.
    window.nodeID = "#{nodeID}"
  script(src='/js/index.js')