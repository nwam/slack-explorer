extends layout

block content
  div(id="viewport" style="width: 100%; height: 100%; border: 1px solid lightgray;")

append scripts
  script.
    window.nodeID = "#{nodeID}"
  script(src='/js/index.js')